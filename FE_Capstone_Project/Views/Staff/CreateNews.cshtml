@model FE_Capstone_Project.Models.NewsCreateModel
@{
    ViewData["Title"] = "Tạo Tin tức Mới";
    Layout = "~/Views/Shared/_StaffLayout.cshtml";
}

<div class="container-fluid">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0 text-dark">@ViewData["Title"]</h1>
        <div>
            <a href="@Url.Action("TestApi", "Staff")" class="btn btn-info me-2" target="_blank">
                <i class="bi bi-bug me-2"></i>Test API
            </a>
            <a href="@Url.Action("News")" class="btn btn-secondary">
                <i class="bi bi-arrow-left me-2"></i>Quay lại
            </a>
        </div>
    </div>

    <!-- Error Message -->
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle me-2"></i>@TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Form -->
    <div class="card shadow-sm">
        <div class="card-body">
            <form method="post" enctype="multipart/form-data">
                <div class="row">
                    <!-- Left column -->
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label asp-for="Title" class="form-label fw-bold">Tiêu đề *</label>
                            <input asp-for="Title" class="form-control" placeholder="Nhập tiêu đề bài viết..." />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="UserId" class="form-label fw-bold">User ID *</label>
                            <input asp-for="UserId" type="number" class="form-control" placeholder="VD: 1" />
                            <span asp-validation-for="UserId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="NewsStatus" class="form-label fw-bold">Trạng thái *</label>
                            <select asp-for="NewsStatus" class="form-select">
                                <option value="">-- Chọn trạng thái --</option>
                                <option value="0">Draft (Nháp)</option>
                                <option value="1">Published (Đã đăng)</option>
                                <option value="2">Archived (Lưu trữ)</option>
                            </select>
                            <span asp-validation-for="NewsStatus" class="text-danger"></span>
                        </div>
                    </div>

                    <!-- Right column -->
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label asp-for="Image" class="form-label fw-bold">Ảnh đại diện (Tùy chọn)</label>
                            <input asp-for="Image" type="file" class="form-control" accept="image/*" name="imageFile" />
                            <small class="text-muted">Chọn ảnh để hiển thị cùng bài viết.</small>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Content" class="form-label fw-bold">Nội dung *</label>
                            <textarea asp-for="Content" class="form-control" rows="6" placeholder="Nhập nội dung chi tiết..."></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <!-- Buttons -->
                <div class="d-flex gap-2 mt-3">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-circle me-2"></i>Tạo Tin Tức
                    </button>
                    <a href="@Url.Action("News")" class="btn btn-secondary">
                        <i class="bi bi-x-circle me-2"></i>Hủy
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
